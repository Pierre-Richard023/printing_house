{% extends 'admin/dashboard.html.twig' %}

{% block title %}
	Type de courier : {{ mailType.name }}
{% endblock %}

{% block body %}


	<div class="relative w-full h-24">
		<div class="absolute flex flex-warp gap-2 top-0 left-0 p-6">
			<a class="bg-secondary text-white px-8 py-3 font-semibold rounded-full" href="{{ path('admin.mailtype.index') }}">Retour</a>
			<a class="bg-primary text-white px-8 py-3 font-semibold rounded-full" href="{{ path('admin.mailtype.edit', {'id': mailType.id}) }}">Editer</a>
		</div>
	</div>

	<div class="bg-white shadow rounded-lg p-4 sm:p-6 xl:p-8 relative">
		<div class="text-xl leading-none font-bold text-gray-900 mb-10">
			Détails du type de courier :
		</div>
		<div class="block w-full md:w-1/2 text-left  overflow-x-auto">

			<table class="min-w-full border-collapse border-spacing-y-2 border-spacing-x-2">
				<tbody class="bg-white lg:border-gray-300">
					<tr>
						<th class="whitespace-no-wrap hidden py-4 text-sm font-normal text-gray-600 sm:px-3 lg:table-cell">Nom</th>
						<td class="whitespace-no-wrap hidden py-4 text-sm font-normal text-gray-600 sm:px-3 lg:table-cell">{{ mailType.name }}</td>
					</tr>
					<tr>
						<th class="whitespace-no-wrap hidden py-4 text-sm font-normal text-gray-600 sm:px-3 lg:table-cell">Prix</th>
						<td class="whitespace-no-wrap hidden py-4 text-sm font-normal text-gray-600 sm:px-3 lg:table-cell">{{ mailType.price }}</td>
					</tr>
				</tbody>
			</table>

		</div>
	</div>

{% endblock %}
