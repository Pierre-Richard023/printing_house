{% extends "base.html.twig" %}

{% block title %}
	Imprimerie - Paiement
{% endblock %}

{% block body %}

	<div {{ react_component('PaymentsController', { 'name' : app.user.firstname ~ ' '~ app.user.lastname } ) }}>

		<div class="h-screen">
			<div class="flex justify-center items-center h-full">
				<div class="flex items-center justify-center space-x-2">
					<div class="w-16 h-16 rounded-full animate-pulse bg-secondary"></div>
					<div class="w-16 h-16 rounded-full animate-pulse bg-secondary"></div>
					<div class="w-16 h-16 rounded-full animate-pulse bg-secondary"></div>
				</div>
			</div>
		</div>

	</div>


	{{ form_start(form) }}
	{{ form_widget(form) }}
	{{ form_end(form) }}

{% endblock %}
