{% set isSelected 	= "border-b-secondary font-semibold text-secondary " %}
{% set notSelected 	= "font-medium text-gray-600 hover:border-b-secondary hover:text-secondary" %}



<div class="mx-auto max-w-screen-xl bg-white">
	<div class="bg-white py-2 px-3">
		<nav class="flex flex-wrap gap-4">
			<a href="{{ path('admin.orders.index') }}" class="inline-flex whitespace-nowrap border-b-2 border-transparent py-2 px-3 text-sm transition-all duration-200 ease-in-out {{ position == 'index' ? isSelected : notSelected }} "  >
				Listes des commandes
			</a>

			<a href="{{ path('admin.orders.waiting') }}" class="inline-flex whitespace-nowrap border-b-2 border-transparent py-2 px-3 text-sm transition-all duration-200 ease-in-out {{ position == 'waiting' ? isSelected : notSelected }}">
				Attente de validation
			</a>

			<a href="{{ path('admin.orders.printing') }}" class="inline-flex whitespace-nowrap border-b-2 border-transparent py-2 px-3 text-sm transition-all duration-200 ease-in-out {{ position == 'printing' ? isSelected : notSelected }}">
				Impression en cours
			</a>

			<a href="{{ path('admin.orders.shipping') }}" class="inline-flex whitespace-nowrap border-b-2 border-transparent py-2 px-3 text-sm transition-all duration-200 ease-in-out {{ position == 'shipping' ? isSelected : notSelected }}">
				Livraison en cours
			</a>
			<a href="{{ path('admin.orders.delivered') }}" class="inline-flex whitespace-nowrap border-b-2 border-transparent py-2 px-3 text-sm transition-all duration-200 ease-in-out {{ position == 'delivered' ? isSelected : notSelected }}">
				Livré
			</a>
		</nav>
	</div>
</div>
