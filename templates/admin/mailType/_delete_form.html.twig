<form method="post" action="{{ path('admin.sendOption.delete', {'id': send_option.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ send_option.id) }}">
    <button class="btn">Delete</button>
</form>
